[
  {
    "file": "app.config.ts",
    "content": "primeng/api primeng/config @primeuix/themes/aura ngx-lottie lottie-web @taiga-ui/core @taiga-ui/core/tokens @taiga-ui/i18n/tokens @taiga-ui/i18n/languages/spanish native assets/taiga-ui/icons"
  },
  {
    "file": "app.routes.ts",
    "content": "ForgotPassword home seguimiento docs administrador format blog admin administrador settings area-detail/:idArea pagetable/:idCurso migracion administrador"
  },
  {
    "file": "app.spec.ts",
    "content": "should create the app should render title Hello, epsas-angular"
  },
  {
    "file": "app.ts",
    "content": "@taiga-ui/core primeng/toast app-root"
  },
  {
    "file": "core\\guards\\auth.guard.ts",
    "content": "/login"
  },
  {
    "file": "core\\guards\\role.guard.ts",
    "content": "roles admin administrador roles"
  },
  {
    "file": "core\\interceptors\\auth.interceptor.ts",
    "content": "centroId cargo /api2/ X-Centro-ID X-Cargo"
  },
  {
    "file": "core\\interceptors\\debug.interceptor.ts",
    "content": "[DEBUG] ${req.method} ${req.url} → status ${event.status} pero ok=false \\nBody: [DEBUG] ${req.method} ${req.url} → ERROR \\nStatus: \\nBody:"
  },
  {
    "file": "core\\services\\api.service.ts",
    "content": "/api /api2 root ${BASE}/areas ${BASE}/area/registrar_jwsv ${BASE}/area/actualizar_jwsv/${id} ${BASE}/area/eliminar_jwsv/${id} ${BASE}/curso/listar_jwsv/${idArea} ${BASE}/curso/registrar_jwsv ${BASE}/curso/actualizar_jwsv/${id} ${BASE}/curso/eliminar_jwsv/${id} ${BASE2}/formatos ${BASE2}/formatos/etapa/${etapaId}?tipo=${tipo} ${BASE2}/formatos/etapa/${etapaId} nombre tipo etapaId file ${BASE2}/formatos ${BASE2}/formatos/${id} ${BASE}/personas instructor administrador instructor administrador ${BASE}/persona/buscar_jwsv/${id} ${BASE}/persona/${id} ${BASE}/persona/registrar_jwsv ${BASE}/matriculas/persona/${idAlumno} ${BASE}/matriculas Error listando todas las matrículas: ${BASE2}/etapa-practica ${BASE2}/etapa-practica ${BASE2}/etapa-practica/${id} ${BASE2}/etapa-practica/${id} ${BASE2}/etapa-practica/${id}/estado ${BASE2}/etapa-practica/${id}/activar ${BASE2}/etapa-practica/${id}/inactivar ${BASE2}/asignaciones/etapa/${etapaId} ${BASE2}/asignaciones ${BASE2}/asignaciones/${id} ${BASE2}/asignaciones/${id} ${BASE2}/observaciones/etapa/${etapaId} ${BASE2}/observaciones/etapa/${etapaId} ${BASE2}/observaciones/seguimiento/${seguimientoId} ${BASE2}/observaciones file ${BASE2}/observaciones/upload/evidencia ${BASE2}/etapa-practica/observacion/${idPractica} ${BASE2}/etapa-practica/avance/${idPractica} ${BASE2}/seguimientos/alumno/${idAlumno} ${BASE2}/seguimientos/etapa/${etapaId} ${BASE2}/seguimiento/actualizar/${id} acta ${BASE2}/seguimiento/acta/${id} ${BASE2}/bitacoras/seguimiento/${idSeguimiento} ${BASE2}/bitacoras/${idBitacora}/estado ${BASE2}/bitacoras file ${BASE2}/bitacoras/${idBitacora}/pdf ${BASE2}/modalidad ${BASE2}/empresas ${BASE}/usuario/registrar_jwsv ${BASE}/credencial/registrar_jwsv ${BASE}/matriculas/${matriculaId}/avance ${BASE2}/configuracion ${BASE2}/configuracion ${BASE}/notificaciones ${BASE}/notificaciones/count ${BASE}/notificaciones/${id}/leer ${BASE}/notificaciones/leer-todas ${BASE}/departamento/recuperar/solicitar ${BASE}/departamento/recuperar/verificar ${BASE}/departamento/recuperar/cambiar"
  },
  {
    "file": "core\\services\\area.service.ts",
    "content": "/api root ${BASE}/areas ${BASE}/area/registrar_jwsv ${BASE}/area/actualizar_jwsv/${id} ${BASE}/area/eliminar_jwsv/${id}"
  },
  {
    "file": "core\\services\\auth.service.ts",
    "content": "root administrador instructor aprendiz administrador user user centroId cargo token user token centroId cargo user token"
  },
  {
    "file": "core\\services\\chat.service.ts",
    "content": "root"
  },
  {
    "file": "core\\services\\curso.service.ts",
    "content": "/api root ${BASE}/curso/listar_jwsv/${idArea} ${BASE}/curso/registrar_jwsv ${BASE}/curso/actualizar_jwsv/${id} ${BASE}/curso/eliminar_jwsv/${id}"
  },
  {
    "file": "core\\services\\excel-parser.service.ts",
    "content": "xlsx root array 6.DEPORTESX Apredices cancelados desertados ACUMULADO Aprendices2025 Aprendices2025111 9. Datos instructores yyyy-mm-dd yyyy-mm-dd Tipo de Documento Número de Documento Nombre Apellidos Estado PROMOVER PRODUCTIVA INGLES TECNICO TOTARAPS ID FICHA FECHA INICIO FECHA LECTIVA FECHA FIN PROGRAMA Fechareporte TOT PROM TOT PROD TOT INGLE TOT TECNICO TOTALRAPS TOTAL RAPS-PROGR % EJECUCION yyyy-mm-dd Tipo de Documento Número de Documento Nombre Apellidos Estado PROMOVER PRODUCTIVA INGLES TECNICO TOTARAPS ID FICHA FECHA INICIO FECHA LECTIVA FECHA FIN PROGRAMA Fechareporte TOT PROM TOT PROD TOT INGLE TOT TECNICO TOTALRAPS % EJECUCION NOMBRE COMPLETO AREA NO.CELULAR CORREO ELECTRONICO ${row.nombre} ${row.apellidos} Activo null"
  },
  {
    "file": "core\\services\\export.service.ts",
    "content": "jspdf jspdf-autotable exceljs root es-CO long numeric long numeric helvetica bold Panel de Control — Etapa Productiva helvetica normal helvetica bold Resumen General Indicador Valor Total Aprendices activos Etapas productivas activas Etapas desertadas Ejecuciones con éxito (certificadas) center bold helvetica bold Distribución Etapa Productiva Indicador Total Porcentaje En etapa productiva (activas) ${etapaActiva.porcentaje}% Otros aprendices Etapas certificadas ${etapaCertificada.porcentaje}% Pendientes por certificar center center bold helvetica bold Listado Detallado de Prácticas Nombre Identificación Ficha Programa Estado Fecha Inicio Fecha Fin Empresa center center center Panel de Control SENA 39A900 E8F5E9 1565C0 E3F2FD F5F5F5 FFFFFF 212121 Resumen portrait A1:C1 🌿 Panel de Control — Etapa Productiva Calibri pattern solid middle center A2:C2 Generado el ${new Date().toLocaleDateString( , { weekday: , year: , month: , day: Calibri pattern solid 2E7D32 center A${fila}:C${fila} A${fila} Calibri pattern solid left Calibri pattern solid center middle thin BDBDBD thin BDBDBD thin BDBDBD thin BDBDBD Calibri pattern solid left center middle hair E0E0E0 hair E0E0E0 thin E0E0E0 thin E0E0E0 📊 Resumen General Indicador Valor Total Aprendices activos Etapas productivas activas Etapas desertadas Ejecuciones con éxito (certificadas) 🍩 Distribución Etapa Productiva Indicador Total Porcentaje En etapa productiva (activas) ${etapaActiva.porcentaje}% Otros aprendices Etapas certificadas ${etapaCertificada.porcentaje}% Pendientes por certificar Prácticas landscape nombre identificacion ficha programa estado fecha_inicio fecha_fin empresaNombre observacion A1:I1 📋 Listado Detallado de Prácticas Calibri pattern solid center middle Nombre Identificación Ficha Programa Estado Fecha Inicio Fecha Fin Empresa Observación Calibri pattern solid 2E7D32 center middle medium medium thin thin activo E8F5E9 activa E8F5E9 certificada E3F2FD certificado E3F2FD desertado FFF3E0 desertada FFF3E0 retirado FFEBEE Calibri pattern solid middle left center hair E0E0E0 hair E0E0E0 thin E0E0E0 thin E0E0E0 Total: ${practicas.length} prácticas A${totalRow.number}:I${totalRow.number} A${totalRow.number} Calibri pattern solid center frozen application/vnd.openxmlformats-officedocument.spreadsheetml.sheet estadisticas_${new Date().toISOString().slice(0, 10)}.xlsx"
  },
  {
    "file": "core\\services\\formato.service.ts",
    "content": "/api root ${BASE}/formatos/listar_jwsv nombre archivo ${BASE}/formatos/subir_jwsv ${BASE}/formatos/eliminar_jwsv/${id}"
  },
  {
    "file": "core\\services\\historial.service.ts",
    "content": "/api /api2 root ${BASE}/personas personas documento Documento vacío ${BASE}/personas/cedula/${cedula} Persona no encontrada ${BASE}/matriculas/persona/${idPersona} matriculas ${BASE2}/etapa-practica/matricula/${idMat} ${BASE2}/asignaciones/etapa/${etapaObj.id} asignaciones ${BASE2}/seguimientos/etapa/${etapaId} seguimientos ${BASE2}/observaciones/etapa/${etapaId} observaciones ${BASE2}/bitacoras/seguimiento/${s.id} bitacoras ${BASE2}/observaciones/seguimiento/${s.id} observaciones Sin programa asignado Curso estado activo Activo graduado Graduado Inactivo estado aprob Aprobado reprob cancel Reprobado En curso En curso Aprobado ${year}-${trimestre}"
  },
  {
    "file": "core\\services\\index.ts",
    "content": "@services"
  },
  {
    "file": "core\\services\\matricula.service.ts",
    "content": "/api root ${BASE}/matriculas/persona/${idAlumno} ${BASE}/matriculas Error listando todas las matrículas:"
  },
  {
    "file": "core\\services\\migration.service.ts",
    "content": "root idle running paused completed error parsing info Analizando archivo: ${file.name} info Archivo cargado. ${total} registros encontrados en ${Object.keys(sheets).length} hojas. idle No hay datos cargados. Carga un archivo Excel primero. running info Iniciando migración de ${totalRows} registros… info Procesando instructores… warning Se detuvo la migración por errores (stopOnError=true). error completed success Migración completada. Creados: ${summary.created} | Actualizados: ${summary.updated} | Omitidos: ${summary.skipped} | Errores: ${summary.errors} paused running idle persona error ficha codigo matricula etapa_practica ficha ficha curso ficha curso ${this.config.apiBaseUrl}/${apiEndpoint}/exists?${params.toString()} skipped ${this.config.apiBaseUrl}/${apiEndpoint}/${realId} updated ${this.config.apiBaseUrl}/${apiEndpoint} created HTTP ${err.status}: ${err.error?.message ?? err.statusText} error Error en ${entity} (${identifier}): ${msg} error id_persona id_ficha id_matricula Activo activo instructor created updated skipped error level file ${this.config.apiBaseUrl}/migrar/personas"
  },
  {
    "file": "core\\services\\persona.service.ts",
    "content": "/api root ${BASE}/personas instructor administrador instructor administrador ${BASE}/persona/buscar_jwsv/${id} ${BASE}/persona/${id} ${BASE}/persona/registrar_jwsv ${BASE}/usuario/registrar_jwsv ${BASE}/credencial/registrar_jwsv ${BASE}/departamento/recuperar/solicitar ${BASE}/departamento/recuperar/verificar ${BASE}/departamento/recuperar/cambiar"
  },
  {
    "file": "core\\services\\practica.service.ts",
    "content": "/api2 root ${BASE2}/etapa-practica ${BASE2}/etapa-practica ${BASE2}/etapa-practica/${id} ${BASE2}/etapa-practica/${id} ${BASE2}/etapa-practica/${id}/activar ${BASE2}/etapa-practica/${id}/inactivar ${BASE2}/etapa-practica/avance/${idPractica} ${BASE2}/etapa-practica/observacion/${idPractica} ${BASE2}/asignaciones/etapa/${etapaId} ${BASE2}/asignaciones ${BASE2}/asignaciones/${id} ${BASE2}/asignaciones/${id} ${BASE2}/modalidad ${BASE2}/empresas"
  },
  {
    "file": "core\\services\\seguimiento.service.ts",
    "content": "/api2 root ${BASE2}/seguimientos/alumno/${idAlumno} ${BASE2}/seguimientos/etapa/${etapaId} ${BASE2}/seguimientos/${id} ${BASE2}/seguimientos/${id}/estado file ${BASE2}/seguimientos/${id}/acta ${BASE2}/bitacoras/seguimiento/${idSeguimiento} ${BASE2}/bitacoras/${idBitacora}/estado ${BASE2}/bitacoras file ${BASE2}/bitacoras/${idBitacora}/pdf ${BASE2}/observaciones/etapa/${etapaId} ${BASE2}/observaciones/etapa/${etapaId} ${BASE2}/observaciones/seguimiento/${seguimientoId} ${BASE2}/observaciones file ${BASE2}/observaciones/upload/evidencia"
  },
  {
    "file": "core\\services\\stats.service.ts",
    "content": "root activa activo en_curso en curso desercion desertada desertado retirado retirada certificada certificado completada completado instructor administrador"
  },
  {
    "file": "core\\services\\theme.service.ts",
    "content": "verde Verde SENA #007832 azul Azul #1e40af indigo Índigo #6366f1 naranja Naranja #f97316 root tema verde #39A900 dark 13px 15px 17px fontSize normal 15px epsas-theme style epsas-theme /* ═══════════════════════════════════════════════════════════ EPSAS Theme — generado por ThemeService Accent: ${accent} Dark: ${darkHex} Light: ${lightHex} ═══════════════════════════════════════════════════════════ */ /* ── Variables globales ── */ :root { font-size: ${fontSize}; --epsas-accent: rgb(${r} ${g} ${b}); --epsas-accent-dark: rgb(${dr} ${dg} ${db}); --epsas-accent-light: ${lightHex}; --tui-primary: rgb(${r} ${g} ${b}); --tui-primary-hover: rgb(${dr} ${dg} ${db}); --tui-background-accent-1: rgb(${r} ${g} ${b}); --tui-text-action: rgb(${r} ${g} ${b}); --tui-border-focus: rgb(${r} ${g} ${b}); } /* ══ bg-[color] ══════════════════════════════════════════ */ .bg-\\\\[\\\\#39A900\\\\], .bg-\\\\[\\\\#2d8400\\\\], .bg-\\\\[\\\\#2d8600\\\\], .bg-\\\\[\\\\#007832\\\\] { background-color: rgb(${r} ${g} ${b}) !important; } .bg-\\\\[\\\\#39A900\\\\]\\\\/5 { background-color: rgb(${r} ${g} ${b} / 0.05) !important; } .bg-\\\\[\\\\#39A900\\\\]\\\\/8 { background-color: rgb(${r} ${g} ${b} / 0.08) !important; } .bg-\\\\[\\\\#39A900\\\\]\\\\/10 { background-color: rgb(${r} ${g} ${b} / 0.10) !important; } .bg-\\\\[\\\\#39A900\\\\]\\\\/20 { background-color: rgb(${r} ${g} ${b} / 0.20) !important; } .bg-\\\\[\\\\#007832\\\\]\\\\/20 { background-color: rgb(${r} ${g} ${b} / 0.20) !important; } /* ══ text-[color] ════════════════════════════════════════ */ .text-\\\\[\\\\#39A900\\\\], .text-\\\\[\\\\#007832\\\\] { color: rgb(${r} ${g} ${b}) !important; } /* ══ border-[color] ══════════════════════════════════════ */ .border-\\\\[\\\\#39A900\\\\] { border-color: rgb(${r} ${g} ${b}) !important; } .border-t-\\\\[\\\\#39A900\\\\] { border-top-color: rgb(${r} ${g} ${b}) !important; } .border-\\\\[\\\\#39A900\\\\]\\\\/20 { border-color: rgb(${r} ${g} ${b} / 0.20) !important; } .border-\\\\[\\\\#39A900\\\\]\\\\/30 { border-color: rgb(${r} ${g} ${b} / 0.30) !important; } .border-\\\\[\\\\#39A900\\\\]\\\\/40 { border-color: rgb(${r} ${g} ${b} / 0.40) !important; } /* ══ ring-[color] ════════════════════════════════════════ */ .ring-\\\\[\\\\#39A900\\\\] { --tw-ring-color: rgb(${r} ${g} ${b}) !important; --tw-ring-shadow: 0 0 0 var(--tw-ring-offset-width, 0px) var(--tw-ring-offset-color, #fff), 0 0 0 calc(1px + var(--tw-ring-offset-width, 0px)) rgb(${r} ${g} ${b}) !important; } /* ══ shadow-[color] ══════════════════════════════════════ */ .shadow-\\\\[\\\\#39A900\\\\] { --tw-shadow-color: rgb(${r} ${g} ${b}) !important; } .shadow-\\\\[\\\\#39A900\\\\]\\\\/20 { --tw-shadow-color: rgb(${r} ${g} ${b} / 0.20) !important; } /* ══ accent-[color] (inputs, checkboxes, etc.) ════════════ */ .accent-\\\\[\\\\#39A900\\\\] { accent-color: rgb(${r} ${g} ${b}) !important; } /* ══ Gradient stops ══════════════════════════════════════ */ .from-\\\\[\\\\#39A900\\\\] { --tw-gradient-from: rgb(${r} ${g} ${b}) !important; } .via-\\\\[\\\\#39A900\\\\] { --tw-gradient-via: rgb(${r} ${g} ${b}) !important; } .to-\\\\[\\\\#39A900\\\\] { --tw-gradient-to: rgb(${r} ${g} ${b}) !important; } .from-\\\\[\\\\#39A900\\\\]\\\\/0 { --tw-gradient-from: rgb(${r} ${g} ${b} / 0) !important; } .from-\\\\[\\\\#39A900\\\\]\\\\/5 { --tw-gradient-from: rgb(${r} ${g} ${b} / 0.05) !important; } .to-\\\\[\\\\#39A900\\\\]\\\\/0 { --tw-gradient-to: rgb(${r} ${g} ${b} / 0) !important; } /* ══ hover: ══════════════════════════════════════════════ */ .hover\\\\:bg-\\\\[\\\\#39A900\\\\]:hover, .hover\\\\:bg-\\\\[\\\\#2d8400\\\\]:hover, .hover\\\\:bg-\\\\[\\\\#2d8600\\\\]:hover { background-color: rgb(${dr} ${dg} ${db}) !important; } .hover\\\\:bg-\\\\[\\\\#39A900\\\\]\\\\/5:hover { background-color: rgb(${r} ${g} ${b} / 0.05) !important; } .hover\\\\:bg-\\\\[\\\\#39A900\\\\]\\\\/10:hover { background-color: rgb(${r} ${g} ${b} / 0.10) !important; } .hover\\\\:text-\\\\[\\\\#39A900\\\\]:hover { color: rgb(${r} ${g} ${b}) !important; } .hover\\\\:text-\\\\[\\\\#acd8a7\\\\]:hover { color: ${lightHex} !important; } .hover\\\\:border-\\\\[\\\\#39A900\\\\]:hover { border-color: rgb(${r} ${g} ${b}) !important; } .hover\\\\:border-\\\\[\\\\#39A900\\\\]\\\\/30:hover { border-color: rgb(${r} ${g} ${b} / 0.30) !important; } .hover\\\\:border-\\\\[\\\\#39A900\\\\]\\\\/50:hover { border-color: rgb(${r} ${g} ${b} / 0.50) !important; } .hover\\\\:from-\\\\[\\\\#39A900\\\\]\\\\/5:hover { --tw-gradient-from: rgb(${r} ${g} ${b} / 0.05) !important; } .hover\\\\:shadow-\\\\[\\\\#39A900\\\\]\\\\/30:hover { --tw-shadow-color: rgb(${r} ${g} ${b} / 0.30) !important; } /* ══ focus: ══════════════════════════════════════════════ */ .focus\\\\:border-\\\\[\\\\#39A900\\\\]:focus { border-color: rgb(${r} ${g} ${b}) !important; } .focus\\\\:border-\\\\[\\\\#39A900\\\\]\\\\/60:focus { border-color: rgb(${r} ${g} ${b} / 0.60) !important; } .focus\\\\:ring-\\\\[\\\\#39A900\\\\]:focus { --tw-ring-color: rgb(${r} ${g} ${b}) !important; box-shadow: 0 0 0 3px rgb(${r} ${g} ${b}) !important; } .focus\\\\:ring-\\\\[\\\\#39A900\\\\]\\\\/20:focus { --tw-ring-color: rgb(${r} ${g} ${b} / 0.20) !important; box-shadow: 0 0 0 3px rgb(${r} ${g} ${b} / 0.20) !important; } .focus\\\\:ring-\\\\[\\\\#39A900\\\\]\\\\/30:focus { --tw-ring-color: rgb(${r} ${g} ${b} / 0.30) !important; box-shadow: 0 0 0 3px rgb(${r} ${g} ${b} / 0.30) !important; } .focus\\\\:ring-\\\\[\\\\#39A900\\\\]\\\\/40:focus { --tw-ring-color: rgb(${r} ${g} ${b} / 0.40) !important; box-shadow: 0 0 0 3px rgb(${r} ${g} ${b} / 0.40) !important; } /* ══ Sidebar ═════════════════════════════════════════════ */ /* routerLinkActive aplica estas clases dinámicamente */ a.bg-\\\\[\\\\#007832\\\\], a[class*= ] { background-color: rgb(${r} ${g} ${b}) !important; } /* ══ Componentes con estilos encapsulados ════════════════ */ /* Estos usan !important para vencer la especificidad Angular */ .toggle.on { background-color: rgb(${r} ${g} ${b}) !important; } .btn-primary { background-color: rgb(${r} ${g} ${b}) !important; } .settings-nav button.active { background-color: rgb(${r} ${g} ${b}) !important; } .strength-seg.filled { background-color: rgb(${r} ${g} ${b}) !important; } .progress-bar-fill { background-color: rgb(${r} ${g} ${b}) !important; } .dot-green { background-color: rgb(${r} ${g} ${b}) !important; } .font-size-btns button.active { border-color: rgb(${r} ${g} ${b}) !important; color: rgb(${r} ${g} ${b}) !important; background: rgb(${r} ${g} ${b} / 0.08) !important; } .color-chip.selected { border-color: #0f172a !important; } /* ══ Sidebar link activo ══════════════════════════════════ */ .nav-link-active { background: rgb(${r} ${g} ${b} / 0.08) !important; color: rgb(${r} ${g} ${b}) !important; border-left-color: rgb(${r} ${g} ${b}) !important; } /* SVG inline */ circle[stroke= ], path[stroke= ] { stroke: rgb(${r} ${g} ${b}) !important; } /* Gradientes compuestos */ .settings-avatar { background: linear-gradient(135deg, rgb(${r} ${g} ${b}), rgb(${dr} ${dg} ${db})) !important; } .personal-hero { background: linear-gradient(135deg, rgb(${r} ${g} ${b}), rgb(${dr} ${dg} ${db})) !important; } .bg-sena-gradient { background: linear-gradient(to right, rgb(${r} ${g} ${b}), rgb(${r} ${g} ${b} / 0.6)) !important; } .accent-card { background: linear-gradient(135deg, rgb(${r} ${g} ${b}), rgb(${dr} ${dg} ${db})) !important; } /* ══ Modo oscuro ═════════════════════════════════════════ */ ${dark ? checkbox radio border-gray bg-black\\\\/ bg-white bg-gray-100 bg-slate-100"
  },
  {
    "file": "core\\services\\toast.service.ts",
    "content": "primeng/api success info warn error MessageService Guardado El registro fue procesado correctamente. Error No se pudo guardar el registro. root success info warn error Ha ocurrido un error inesperado. Error"
  },
  {
    "file": "features\\admin\\admin-panel\\admin-panel.component.ts",
    "content": "primeng/toast primeng/confirmdialog primeng/api app-admin-panel Panel Administrativo Gestión de personas, matrículas, cursos y estructura académica @for (mod of modulos; track mod) { @if (admin.activeTab() === mod) { } } @if (admin.filtro()) { } Filas 10 20 50 100 @if (config[admin.activeTab()].crear) { Agregar } @if (!admin.loading() && admin.totalRegistros() > 0) { Mostrando de registros p - 1) admin.paginaActual() === 1 p-2.5 rounded-lg bg-white border border-gray-300 text-gray-600 hover:bg-[#39A900] hover:text-white hover:border-[#39A900] disabled:opacity-30 disabled:hover:bg-white disabled:hover:text-gray-600 shadow-sm hover:shadow-md transition-all duration-200 [disabled]= class="
  },
  {
    "file": "features\\admin\\components\\admin-modal.component.ts",
    "content": "app-admin-modal @if (open) { × @for (col of columns; track col) { @if (esAutocomplete(col)) { @if (listasAbiertas[col]) { @if (filtradas(col).length === 0) { Sin resultados } @for (opt of filtradas(col); track opt.value) { } } } @else if (opciones[col]?.length) { — Selecciona — @for (opt of opciones[col]; track opt.value) { } } @else { } } @if (error) { } Cancelar } registro"
  },
  {
    "file": "features\\admin\\components\\admin-table.component.ts",
    "content": "app-admin-table @if (loading) { } @else if (rows.length === 0) { No hay registros } @else { @for (col of visibleColumns; track col) { } Acciones @for (row of rows; track $index) { @for (col of visibleColumns; track col) { } @if (canEdit) { } @if (canDelete) { } } } idPersona"
  },
  {
    "file": "features\\admin\\config\\admin.config.ts",
    "content": "/api personas matriculas cursos programas areas sedes centros usuarios credenciales roles municipios aplicativos departamentos text date number email Departamentos idDepartamento ${BASE}/departamentos Personas idPersona ${BASE}/personas ${BASE}/personas ${BASE}/personas/${id} ${BASE}/personas/${id} nombre cedula telefono municipioId direccion correo genero cargo estado municipios nombre idMunicipio number number email Matrículas idMatricula ${BASE}/matriculas ${BASE}/matriculas ${BASE}/matriculas/${id} ${BASE}/matriculas/${id} estudiante curso persona curso personas nombre idPersona cursos codigo idCurso Cursos idCurso ${BASE}/cursos ${BASE}/cursos ${BASE}/curso/actualizar_jwsv/${id} ${BASE}/curso/eliminar_jwsv/${id} codigo fechaInicio fechaFin finLectiva areaId programaId liderId areas nombre idArea programas nombre idPrograma personas nombre idPersona date date date Programas idPrograma ${BASE}/programas ${BASE}/programas ${BASE}/programa/actualizar_jwsv/${id} ${BASE}/programas/${id} nombre tipo Áreas idArea ${BASE}/areas ${BASE}/areas ${BASE}/area/actualizar_jwsv/${id} ${BASE}/area/eliminar_jwsv/${id} nombre sedeId sedes nombre idSede Sedes idSede ${BASE}/sedes ${BASE}/sedes ${BASE}/sede/actualizar_jwsv/${id} ${BASE}/sede/eliminar_jwsv/${id} nombre centroFormacionId centros nombre idCentro Centros idCentro ${BASE}/centro-formacion ${BASE}/centro-formacion ${BASE}/centro/actualizar_jwsv/${id} ${BASE}/centro/elimimar_jwsv/${id} nombre direccion municipioId municipios nombre idMunicipio Usuarios idUsuario ${BASE}/usuarios ${BASE}/usuarios ${BASE}/usuario/actualizar_jwsv/${id} ${BASE}/usuario/eliminar_jwsv/${id} persona aplicativo personaId aplicativoId personas nombre idPersona aplicativos nombre idAplicativo Credenciales idCredencial ${BASE}/credenciales ${BASE}/credenciales ${BASE}/credencial/actualizar_jwsv/${id} ${BASE}/credencial/eliminar_jwsv/${id} login usuario login password rolId usuarioId roles nombre idRol usuarios persona idUsuario Roles idRol ${BASE}/roles ${BASE}/roles ${BASE}/rol/actualizar_jwsv/${id} ${BASE}/rol/eliminar_jwsv/${id} nombre aplicativo nombre aplicativoId aplicativos nombre idAplicativo Municipios idMunicipio ${BASE}/municipios ${BASE}/municipio/registrar_jwsv ${BASE}/municipio/actualizar_jwsv/${id} ${BASE}/municipio/eliminar_jwsv/${id} nombre departamentoId departamentos nombre idDepartamento Aplicativos idAplicativo ${BASE}/aplicativos ${BASE}/aplicativo/registrar_jwsv ${BASE}/aplicativo/actualizar_jwsv/${id} ${BASE}/aplicativo/eliminar_jwsv/${id} nombre departamentos"
  },
  {
    "file": "features\\admin\\services\\admin.service.ts",
    "content": "primeng/api personas password token secret departamentos municipios centros sedes areas programas personas cursos departamentos municipios centros sedes areas programas personas cursos object string matriculas credenciales [Admin] Error cargando ${mod}: string success Actualizado Registrado El registro fue procesado correctamente. Error al guardar. error Error ¿Estás seguro? Esta acción no se puede deshacer. Atención pi pi-exclamation-triangle Cancelar secondary Sí, eliminar danger success Eliminado Registro eliminado correctamente. No se pudo eliminar. error No se pudo eliminar"
  },
  {
    "file": "features\\auth\\forgot-password\\forgot-password.component.ts",
    "content": "app-forgot-password ¿Olvidaste tu Contraseña? No te preocupes, sigue las instrucciones para restablecer tu contraseña. Volver al inicio de sesión Recuperar contraseña {{ step() === 1 ? : step() === 2 ? }} Correo Electronico @if (mensaje()) { } @if (error()) { } @if (step() === 1) { Enviar código } @if (step() === 2) { Verificar código } @if (step() === 3) { Cambiar contraseña } Código enviado a tu correo. No se pudo enviar el código. Código correcto. Ingresa tu nueva contraseña. Código incorrecto. Contraseña actualizada. Ya puedes iniciar sesión. No se pudo cambiar la contraseña."
  },
  {
    "file": "features\\auth\\login\\login.component.ts",
    "content": "app-login @for (p of particles; track p.id) { } @for (s of stars; track s.id) { } EPSAS Sistema de Prácticas · SENA S Bienvenido de nuevo Gestiona las etapas prácticas desde un solo lugar © SENA · Centro Yamboró · Todos los derechos reservados Iniciar sesión Ingresa tus credenciales para continuar @if (error()) { } Usuario Contraseña @if (showPassword) { } @else { } ¿Olvidaste tu contraseña? @if (loading()) { Ingresando… } @else { Ingresar } EPSAS · Etapas Prácticas · Centro Yamboró width:${size}px;height:${size}px; top:${(Math.random() * 100).toFixed(1)}%; left:${(Math.random() * 82).toFixed(1)}%; background:rgba(255,255,255,${(0.2 + Math.random() * 0.6).toFixed(2)}); animation:twinkle ${dur}s ${delay}s infinite alternate; width:${size}px;height:${size}px; top:${(Math.random() * 100).toFixed(1)}%; left:${(Math.random() * 100).toFixed(1)}%; background:rgba(57,169,0,${(0.04 + Math.random() * 0.06).toFixed(2)}); animation:floatP ${(5 + Math.random() * 6).toFixed(1)}s ${delay}s infinite ease-in-out alternate; /home Usuario o contraseña incorrectos. Intenta de nuevo."
  },
  {
    "file": "features\\chat\\chat.component.ts",
    "content": "user assistant app-chat MiChats @for (msg of messages(); track $index) { @if (msg.role === ) { } @if (msg.role === ) { } } @if (isLoading()) { } anchor assistant ¡Hola! Soy tu asistente. ¿En qué puedo ayudarte hoy? smooth user gpt-3.5-turbo system Soy un asistente inteligente que te ayudará en tus preguntas sobre el mundo de la programación. Lo siento, no obtuve respuesta. assistant assistant Lo siento, no fue posible conectar. Revisa tu configuración e intenta nuevamente."
  },
  {
    "file": "features\\formatos\\formatos.component.ts",
    "content": "contrato Contrato acta_inicio Acta de inicio acta_seguimiento_1 Acta seguimiento 1 acta_seguimiento_2 Acta seguimiento 2 carta_presentacion Carta de presentación paz_y_salvo Paz y salvo certificado Certificado otro Otro app-formatos Formatos @if (esAdmin()) { Subir nuevo formato Nombre descriptivo Tipo de formato Selecciona un tipo @for (t of tipos; track t.value) { } Archivo } Archivos disponibles @if (loading()) { } @if (!loading() && formatos().length === 0) { No hay formatos registrados } @for (f of formatos(); track f.id) { 📄 Ver Descargar @if (esAdmin()) { Eliminar } } No se pudieron cargar los formatos. Campo requerido Selecciona un archivo. Campo requerido Escribe un nombre descriptivo. Campo requerido Selecciona el tipo de formato. Archivo subido El formato fue registrado correctamente. Error al subir el archivo. ¿Eliminar este formato? Eliminado Formato eliminado correctamente. Error al eliminar el formato."
  },
  {
    "file": "features\\historial\\historial.component.ts",
    "content": "idle loading success error app-historial Historial del aprendiz @if (estado === ) { } @else if (query()) { × } @if (mostrarSugerencias() && sugerencias().length) { @if (cargandoPersonas()) { Cargando aprendices... } @else { @for (p of sugerencias(); track p.idPersona ?? p.id_persona) { @if (p.programa) { · } } } } @if (estado === ) { Ingresa la cédula del aprendiz para ver su historial. } @if (estado === ) { Cargando historial de … } @if (estado === ) { No se encontró ningún aprendiz con ese documento. } @if (estado === && resultado) { Documento Matrículas Email Teléfono Historial académico Cursos Prácticas Bitácoras Observaciones @if (resultado.historial.length) { Ficha Programa Periodo Estado @for (curso of resultado.historial; track curso.idMatricula) { } } @else { Sin matrículas registradas. } Etapa práctica @if (resultado.practicas.length) { @for (p of resultado.practicas; track p.id) { Inicio Fin Empresa Modalidad @if (p.observacion) { } @if (p.asignaciones.length) { Instructores asignados @for (a of p.asignaciones; track a.id) { · h asignadas → } } @if (p.seguimientos.length) { @for (s of p.seguimientos; track s.id; let i = $index) { Seguimiento # @if (s.actasPdf) { 📎 Acta } → @if (s.observacion) { } @if (s.bitacoras.length) { Bitácoras ( ) @for (b of s.bitacoras; track b.id) { — @if (b.pdf) { · PDF } } } @else { Sin bitácoras. } @if (s.observaciones.length) { Observaciones ( ) @for (obs of s.observaciones; track obs.id) { } } } } @else { Sin seguimientos. } } } @else { El aprendiz no tiene etapa práctica registrada. } } idle document:click loading success error idle idle TOTAL PERSONAS:"
  },
  {
    "file": "features\\home\\home.component.ts",
    "content": "app-home es-CO long numeric long numeric aprendiz instructor ${(porcentaje / 100) * c} ${c} activo activa en_curso en curso badge-green certificado certificada badge-blue desercion desertado desertada badge-red suspendido suspendida badge-orange badge-gray administrador instructor ${persona.nombres ?? persona.nombre ?? } ${persona.apellidos ?? persona.apellido ?? Error cargando datos para home: Error No se pudieron cargar los datos del panel. PDF generado El reporte fue exportado correctamente. Error No se pudo generar el PDF. Excel generado El reporte fue exportado correctamente. Error No se pudo generar el archivo Excel."
  },
  {
    "file": "features\\migracion\\migration.component.ts",
    "content": "idle running completed error app-migration Migración de Base de Datos SENA – Centro Yamboró · El proceso corre en el servidor @if (mostrarNotificacion()) { Duración: · Personas: nuevas, actualizadas · Errores: × } 1 · Seleccionar archivo Excel @if (!archivo()) { 📂 Arrastra el archivo aquí o haz clic para seleccionar Formatos aceptados: .xlsx · .xls · máx. 50 MB } @else { 📊 MB · clic para cambiar } @if (subiendo()) { Subiendo archivo… } @else if (estado().status === ) { 🔄 Migración en curso… } @else { 🚀 Iniciar Migración } @if (estado().status === || estado().status === ) { Nueva migración } @if (estado().status !== ) { 2 · Progreso en tiempo real @if (estado().status === ) { ⏳ Procesando en el servidor… puedes seguir trabajando } @for (line of logsVisibles(); track $index) { } @if (estado().status === ) { █ } @if (estado().resumen && estado().status !== ) { 3 · Resumen @for (item of resumenItems(); track item.label) { } @if (estado().resumen!.errores > 0) { ⚠ Hubo errores durante la migración. Revisa el archivo scripts/migrate_aprendices.log en el servidor para ver el detalle. } @if (aprendicesHabilitados().length > 0) { 🎓 aprendiz habilitado para etapa práctica Avance ≥ % @for (a of aprendicesHabilitados(); track a.cedula) { % avance } Estos aprendices pueden ser vinculados a una etapa práctica desde la sección de Seguimiento. } @else if (estado().status === ) { 🔔 Ningún aprendiz alcanzó el % de avance mínimo aún. } } } idle completed error Personas nuevas bg-blue-50 text-blue-700 Personas actualizadas bg-indigo-50 text-indigo-700 Cursos (fichas) bg-purple-50 text-purple-700 Matrículas bg-green-50 text-green-700 Programas bg-yellow-50 text-yellow-700 Omitidas bg-gray-50 text-gray-600 Errores bg-red-50 text-red-700 bg-green-50 text-green-700 bg-gray-100 text-gray-600 bg-blue-600 text-white animate-pulse bg-green-100 text-green-700 bg-red-100 text-red-700 idle Listo Procesando… Completado Con errores Listo /api/migracion/estado running completed error completed aprendices_habilitados /api/migracion/estado completed error completed running file minAvance /api/migracion/iniciar running error Error al iniciar: ${err?.error?.message ?? err?.message ?? /api/migracion/resetear idle [ERROR] ERROR text-red-400 [INFO] text-green-300 Completada completad text-green-400 font-semibold WARNING text-yellow-300 text-gray-400"
  },
  {
    "file": "features\\migracion\\models\\migration.models.ts",
    "content": "persona programa ficha matricula etapa_practica instructor created updated skipped error idle parsing running paused completed error info warning error success /api"
  },
  {
    "file": "features\\not-found\\not-found.component.ts",
    "content": "app-not-found cursor mousemove clock"
  },
  {
    "file": "features\\seguimiento\\components\\bitacoras-modal.component.ts",
    "content": "ng2-pdf-viewer app-bitacoras-modal @if (isOpen && alumno) { Bitácoras de registros @if (loading()) { @for (i of [1,2,3,4]; track i) { } } @else if (bitacoras().length === 0) { No hay bitácoras registradas } @else { @for (item of bitacoras(); track (item.id || $index)) { @if (item.bitacora_pdf && item.bitacora_pdf !== ) { } @else { Sin documento } @if (uploadingId() === item.id) { Subiendo... } @else if (!item.bitacora_pdf || item.bitacora_pdf === ) { Subir PDF } @else { Reemplazar } @if (canEvaluar()) { Evaluar } } } Cerrar @if (openDropdown() && dropdownPos()) { 🕒 Pendiente ✅ Aceptar ❌ Rechazar } @if (detalleOpen() && bitacoraSeleccionada()) { Bitácora — ‹ / = pdfTotalPages text-white/70 hover:text-white disabled:opacity-30 px-1 ]); } @Input() isOpen = false; @Input() alumno: any = null; @Input() seguimiento: any = null; @Input() practicaId: any = null; // id de etapa_practica para actualizar avance @Output() closed = new EventEmitter (); @Output() avanceActualizado = new EventEmitter (); // Agrega estas propiedades en la clase: dropdownPos = signal (null); // ← ViewChild resuelve el input fuera de bloques @if @ViewChild( ) onDocumentClick(): void { this.openDropdown.set(null); this.dropdownPos.set(null); } // ── PDF Viewer ──────────────────────────────────────────────────────────── pdfPage = 1; pdfTotalPages = 1; pdfZoom = 1.0; pdfUrl(archivo: string): string { return ; // pasa por el proxy de Angular → sin CORS } onPdfLoaded(pdf: any): void { this.pdfTotalPages = pdf.numPages; this.pdfPage = 1; } prevPage(): void { if (this.pdfPage > 1) this.pdfPage--; } nextPage(): void { if (this.pdfPage { const id = this.seguimiento?.id ?? this.seguimiento?.id_seguimiento; if (!id) return; this.loading.set(true); try { const data = await this.seguimientoSvc.obtenerBitacoras(id); this.bitacoras.set(data || []); } catch (e) { this.bitacoras.set([]); } finally { this.loading.set(false); } } abrirDetalle(item: any): void { this.bitacoraSeleccionada.set(item); this.detalleOpen.set(true); } // Reemplaza toggleDropdown: toggleDropdown(item: any, event: MouseEvent): void { if (this.openDropdown() === item.id) { this.openDropdown.set(null); this.dropdownPos.set(null); return; } const btn = event.currentTarget as HTMLElement; const rect = btn.getBoundingClientRect(); this.dropdownPos.set({ top: rect.bottom + 6, left: rect.right - 160 // 160 = min-width del menú }); this.openDropdown.set(item.id); } async cambiarEstado(item: any, estado: string): Promise { this.openDropdown.set(null); this.dropdownPos.set(null); const bitacoraId = item.id ?? item.id_bitacora; try { await this.seguimientoSvc.actualizarEstadoBitacora(bitacoraId, estado); this.bitacoras.update(lista => lista.map(b => (b.id ?? b.id_bitacora) === bitacoraId ? { ...b, estado } : b) ); this.recalcularAvance(); } catch (e) { await this.cargarBitacoras(); } } private recalcularAvance(): void { if (this.practicaId == null) return; // El backend suma las bitácoras aceptadas de TODOS los seguimientos // de la etapa práctica (no solo las del seguimiento actual). this.bitacoraSvc.actualizarAvancePractica(this.practicaId) .then((res: any) => { const avance = res?.avance ?? 0; console.log( ); this.avanceActualizado.emit(avance); }) .catch(e => console.error( , e)); } cambiarEstadoById(id: string, estado: string): void { const item = this.bitacoras().find(b => (b.id ?? b.id_bitacora) === id); if (item) this.cambiarEstado(item, estado); } formatFecha(fecha: string): string { if (!fecha) return ; const d = new Date(fecha); return isNaN(d.getTime()) ? : d.toLocaleDateString( ); } /** Abre el file-picker asociando la bitácora a subir */ seleccionarPdf(item: any): void { this.pendingUploadItem = item; const input = this.pdfInputRef?.nativeElement; if (!input) return; input.value ="
  },
  {
    "file": "features\\seguimiento\\components\\crear-practica-modal.component.ts",
    "content": "@taiga-ui/cdk @taiga-ui/core practicaId practicaId Gestionar asignaciones app-crear-practica-modal @if (isOpen) { @if (modoEditar && alumnoPreseleccionado) { · Modifica los datos de la etapa práctica } @else if (modoEditar) { Modifica los datos de la etapa práctica } @else { Completa los datos para asignar la etapa práctica } @if (loadingData()) { Cargando datos... } @else { @if (alumnoPreseleccionado) { · ID: } @if (!modoEditar && !alumnoPreseleccionado) { Aprendiz Selecciona un aprendiz @for (a of sinPractica(); track a.id) { — } } Modalidad Selecciona una modalidad @for (m of modalidades(); track m.id) { } Empresa Selecciona una empresa @for (e of empresas(); track e.id) { } Fecha inicio {{ fechaInicioCtrl.value ? (fechaInicioCtrl.value.day + + (fechaInicioCtrl.value.month + 1) + + fechaInicioCtrl.value.year) : }} @if (calAbierto === ) { } Fecha fin {{ fechaFinCtrl.value ? (fechaFinCtrl.value.day + + (fechaFinCtrl.value.month + 1) + + fechaFinCtrl.value.year) : }} @if (calAbierto === ) { } @if (!modoEditar) { Instructor responsable (opcional) @if (mostrarListaInstructor && instructoresFiltrados().length > 0) { @for (inst of instructoresFiltrados(); track inst.idPersona ?? inst.id_persona) { } } @if (mostrarListaInstructor && instructoresFiltrados().length === 0 && instructorTexto.length > 0) { Sin resultados para } @if (form.instructorId) { Horas asignadas al instructor } } Estado Activo Inactivo Suspendido Certificado Por certificar Observación (opcional) @if (error()) { } } Cancelar @if (loading()) { } @else { } } inicio activo isOpen aprendices activo Error cargando datos. activo inicio inicio inicio ${day.year}-${m}-${d} Selecciona una modalidad. Selecciona una empresa. Ingresa la fecha de inicio. Ingresa la fecha de fin. Etapa actualizada Etapa creada Los cambios fueron guardados correctamente. La etapa práctica fue creada correctamente. string Error al guardar los cambios. Error al crear la etapa práctica. Error Selecciona un aprendiz. sin aprendiz El aprendiz no tiene matrícula registrada. sin matricula"
  },
  {
    "file": "features\\seguimiento\\components\\filters\\area-filter.component.ts",
    "content": "app-area-filter Área @if(selected.length){ } @if(show()){ Filtrar por área @for(area of areas; track area){ } Limpiar }"
  },
  {
    "file": "features\\seguimiento\\components\\filters\\estado-filter.component.ts",
    "content": "app-estado-filter Estado @if(selected.length){ } @if(show()){ Filtrar por estado @for(status of options; track status.uid){ } Limpiar }"
  },
  {
    "file": "features\\seguimiento\\components\\observacion-modal.component.ts",
    "content": "app-observacion-modal @if (isOpen && alumno) { Registrar observación Seguimiento @if (cargandoSeguimientos()) { Cargando seguimientos... } @else if (seguimientos().length === 0) { No hay seguimientos registrados para esta etapa. } @else { — Selecciona un seguimiento — @for (seg of seguimientos(); track seg.id; let i = $index) { Seguimiento # — } } @if (seguimientoSeleccionadoId) { @if (cargandoHistorial()) { Cargando observaciones anteriores... } @else if (historial().length > 0) { Observaciones anteriores @for (obs of historial(); track obs.id) { @if (obs.evidencia_foto) { Ver evidencia } } } @else { Sin observaciones para este seguimiento. } Nueva observación Evidencia fotográfica (opcional · JPG, PNG, WEBP · máx 5 MB) @if (!fotoPreview()) { Haz clic para seleccionar una foto } @else { } @if (errorFoto()) { } @if (error()) { } } Cancelar @if (loading()) { } @else { Guardar observación } } fileInput isOpen La imagen no puede superar los 5 MB. La observación no puede estar vacía. Selecciona un seguimiento primero. No se pudo identificar al usuario. Por favor recarga la sesión. Observación guardada La observación fue registrada correctamente. string Error al guardar la observación. Error ${window.location.origin}${url} _blank noopener,noreferrer es-CO numeric short numeric"
  },
  {
    "file": "features\\seguimiento\\components\\pagination\\table-pagination.component.ts",
    "content": "app-table-pagination ‹ @for(p of pagesArray(); track p){ } ›"
  },
  {
    "file": "features\\seguimiento\\components\\seguimientos-modal.component.ts",
    "content": "app-seguimientos-modal @if (isOpen && alumno) { Seguimientos de × @if (loading()) { } @else if (seguimientos().length === 0) { No hay seguimientos registrados } @else { @for (item of seguimientos(); track item.id) { Seguimiento @if (canGestionarSeguimiento()) { ⋮ @if (openMenu() === item.id) { Cambiar estado @for (est of ESTADOS; track est.key) { @if (item.estado === est.key) { } } 📤 Subir / Reemplazar Acta 📥 Descargar Acta } } @if (item.actas_pdf) { 📎 Acta cargada — ver PDF } @else { ⚠ Sin acta adjunta } } } Cerrar @if (editOpen()) { Editar Seguimiento Cancelar Guardar } } administrador instructor activo Activo bg-green-500 text-green-700 pendiente Pendiente bg-yellow-500 text-yellow-700 inactivo Inactivo bg-red-500 text-red-600 bg-green-100 text-green-700 activo bg-yellow-100 text-yellow-700 pendiente bg-red-100 text-red-600 inactivo bg-gray-100 text-gray-500 activo pendiente inactivo isOpen Seguimiento actualizado La observación fue guardada correctamente. Error al guardar el seguimiento. Estado actualizado El seguimiento cambió a Error al cambiar el estado. input file application/pdf Acta subida El acta PDF fue cargada correctamente. Error al subir el acta. _blank"
  },
  {
    "file": "features\\seguimiento\\components\\table\\aprendices-row.component.ts",
    "content": "app-aprendices-row 📄 ✏️ ➕"
  },
  {
    "file": "features\\seguimiento\\components\\table\\aprendices-table.component.ts",
    "content": "app-aprendices-table @for(col of columns; track col.key){ } @for(row of rows; track row.id){ }"
  },
  {
    "file": "features\\seguimiento\\components\\table-info\\table-body.component.ts",
    "content": "app-table-body display: contents @if (rows().length === 0) { No hay registros } @for (item of rows(); track item.id; let odd = $odd) { @for (col of columns(); track col.uid) { @switch (col.uid) { @case ( ) { } @case ( ) { @if (!item.id_practica) { ! Sin práctica } @else { } } @case ( ) { @if (item.observacion) { } @else { — } } @case ( ) { @if (item.id_practica) { % } @else { — } } @case ( ) { @if (canObservar()) { @if (item.id_practica) { } @else { } } @if (canCrearPractica() && item.id_practica) { } @if (canCrearPractica() && item.id_practica) { @if (openEstadoMenu() === String(item.id_practica)) { @for (est of ESTADOS; track est.key) { @if (item.estado === est.key) { } } } } @if (canCrearPractica() && item.id_practica) { } @if (canCrearPractica() && !item.id_practica) { @if (item.avance_matricula >= minAvance()) { } @else { } } } @default { } } } } activo Activo bg-green-500 text-green-700 inactivo Inactivo bg-red-500 text-red-600 suspendido Suspendido bg-yellow-500 text-yellow-700 por certificar Por certificar bg-indigo-500 text-indigo-600 certificado Certificado bg-blue-500 text-blue-600 document:click administrador instructor administrador bg-green-100 text-green-700 activo bg-red-100 text-red-600 inactivo bg-yellow-100 text-yellow-700 suspendido bg-indigo-100 text-indigo-600 por certificar bg-blue-100 text-blue-600 certificado bg-gray-100 text-gray-500 activo inactivo suspendido por certificar certificado bg-green-500 activo bg-red-500 inactivo bg-yellow-500 suspendido bg-indigo-500 por certificar bg-blue-500 certificado bg-gray-400 activo inactivo suspendido por certificar certificado #39A900 #f5a524 #f31260 actions"
  },
  {
    "file": "features\\seguimiento\\components\\table-info\\table-header.component.ts",
    "content": "app-table-header display: contents @for (col of columns(); track col.uid) { @if (col.uid === ) { 0 selectedAreas().length > 0 selectedAreas().length === 0 #39A900 none [attr.stroke]= #39A900 currentColor stroke-width= > Área @if (selectedAreas().length > 0) { } @if (showAreaMenu) { Filtrar por área @for (area of areas(); track area) { } @if (selectedAreas().length > 0) { Limpiar filtro } } } @else if (col.uid === ) { 0 selectedStatuses().length > 0 selectedStatuses().length === 0 #39A900 none [attr.stroke]= #39A900 currentColor stroke-width= > Estado @if (selectedStatuses().length > 0) { } @if (showStatusMenu) { Filtrar por estado @for (s of statusOptions; track s.uid) { } @if (selectedStatuses().length > 0) { Limpiar filtro } } } @else { @if (col.sortable && sortCol() === col.uid) { } } } document:click"
  },
  {
    "file": "features\\seguimiento\\components\\table-info\\table-info.component.ts",
    "content": "app-table-info Aprendices @if (loading()) { } @else { } name desc desc actions aprendiz instructor [TableInfo] Error: desc [TableInfo] Error cambiando estado: [TableInfo] Error actualizando avance:"
  },
  {
    "file": "features\\seguimiento\\components\\table-info\\table-info.types.ts",
    "content": "actions Nombre name Identificación Email email Área area Programa programa Ficha number Fecha Inicio startDate Fecha Fin endDate Observación observacion Prog. Académico avance_matricula % Práctica avance Estado estado Acciones actions name area programa number startDate endDate observacion avance_matricula avance estado actions Activo activo Inactivo inactivo Suspendido suspendido es-CO numeric 2-digit 2-digit bg-blue-100 text-blue-600 bg-purple-100 text-purple-600 bg-orange-100 text-orange-600 bg-pink-100 text-pink-600 bg-teal-100 text-teal-600 bg-yellow-100 text-yellow-600 #39A900 #f5a524 #f31260 #e4e4e7 #39A900 #f31260 #f5a524 #ccc"
  },
  {
    "file": "features\\seguimiento\\components\\table-info\\table-pagination.component.ts",
    "content": "app-table-pagination @if (pages() > 0) { 1 && pageChange.emit(page() - 1) page() === 1 px-2.5 py-1.5 text-xs rounded-lg border border-gray-200 text-gray-500 hover:border-[#39A900] disabled:opacity-40 transition-colors"
  },
  {
    "file": "features\\seguimiento\\components\\table-info\\table-toolbar.component.ts",
    "content": "app-table-toolbar @if (filter()) { × } Columnas @if (showColMenu) { Columnas visibles @for (col of allColumns; track col.uid) { @if (col.uid !== ) { } } } Total aprendices Filas por página: 5 10 15 20 document:click"
  },
  {
    "file": "features\\seguimiento\\components\\table-info.component.ts",
    "content": "Nombre name Identificación Email email Área area Programa programa Ficha number Fecha Inicio startDate Fecha Fin endDate Observación observacion Avance avance Estado estado Acciones actions name area programa number startDate endDate observacion avance estado actions Activo activo Inactivo inactivo Suspendido suspendido es-CO numeric 2-digit 2-digit app-table-info // // // // Aprendices // // // // // // // // @if (filterValue()) { // × // } // // // // // // Columnas // // // // // @if (showColMenu) { // // Columnas visibles // @for (col of allColumns; track col.uid) { // @if (col.uid !== ) { // // // // // } // } // // } // // // // // // Total aprendices // // Filas por página: // // 5 // 10 // 15 // 20 // // // // // // // @if (loading()) { // // // // } @else { // // // // @for (col of headerColumns(); track col.uid) { // // @if (col.uid === ) { // // // 0 selectedAreas().length > 0 selectedAreas().length === 0 #39A900 none // [attr.stroke]= #39A900 currentColor stroke-width= > // // // Área // @if (selectedAreas().length > 0) { // // // // } // // // // // @if (showAreaMenu) { // // Filtrar por área // // @for (area of areas(); track area) { // // // // // } // // @if (selectedAreas().length > 0) { // // // Limpiar filtro // // // } // // } // // } @else if (col.uid === ) { // // // 0 selectedStatuses().length > 0 selectedStatuses().length === 0 #39A900 none // [attr.stroke]= #39A900 currentColor stroke-width= > // // // Estado // @if (selectedStatuses().length > 0) { // // // // } // // // // // @if (showStatusMenu) { // // Filtrar por estado // // @for (s of statusOptions; track s.uid) { // // // // // // } // // @if (selectedStatuses().length > 0) { // // // Limpiar filtro // // // } // // } // // } @else { // // // @if (col.sortable && sortCol === col.uid) { // // } // // } // // } // // // // @if (paged().length === 0) { // // // No hay registros // // // } // @for (item of paged(); track item.id; let odd = $odd) { // // @for (col of headerColumns(); track col.uid) { // // @switch (col.uid) { // @case ( ) { // // // // // // // // // // } // @case ( ) { // @if (!item.id_practica) { // // ! Sin práctica // // } @else { // // // // // // } // } // @case ( ) { // @if (item.observacion) { // // // // } @else { // — // } // } // @case ( ) { // @if (item.id_practica) { // // // // // // // % // // // } @else { // — // } // } // @case ( ) { // // // // // // // // // // // // // @if (item.id_practica) { // // // // // // // } @else { // // // // // // // } // // @if (!item.id_practica) { // // // // // // // // } @else { // // // // // // // // } // // } // @default { // // } // } // // } // // } // // // } // // // @if (pages() > 0) { // // 1 && page.set(page() - 1) page() === 1 px-2.5 py-1.5 text-xs rounded-lg border border-gray-200 text-gray-500 // hover:border-[#39A900] disabled:opacity-40 transition-colors ; // sortDir: ; // visibleCols = new Set(INITIAL_VISIBLE); // showColMenu = false; // showAreaMenu = false; // showStatusMenu = false; // allColumns = COLUMNS; // statusOptions = STATUS_OPTIONS; // // Modales // modalSeguimientos = false; // modalObservacion = false; // modalCrearPractica = false; // alumnoSeleccionado: any = null; // alumnoObservacion: any = null; // alumnoParaPractica: any = null; // // Computed // filtered = computed(() => { // let rows = this.data(); // const filter = this.filterValue(); // const areas = this.selectedAreas(); // const statuses = this.selectedStatuses(); // if (filter) { // rows = rows.filter(r => // r.name?.toLowerCase().includes(filter.toLowerCase()) || // String(r.age ?? ).includes(filter) || // String(r.number ?? ).includes(filter) // ); // } // if (statuses.length > 0) { // rows = rows.filter(r => statuses.includes(r.estado)); // } // if (areas.length > 0) { // rows = rows.filter(r => areas.includes(r.area)); // } // return rows; // }); // pages = computed(() => // Math.ceil(this.filtered().length / this.rowsPerPage()) // ); // paged = computed(() => { // const start = (this.page() - 1) * this.rowsPerPage(); // const slice = this.filtered().slice(start, start + this.rowsPerPage()); // return [...slice].sort((a, b) => { // const fa = a[this.sortCol], fb = b[this.sortCol]; // const cmp = fa fb ? 1 : 0; // return this.sortDir === ? -cmp : cmp; // }); // }); // headerColumns = computed(() => // COLUMNS.filter(c => c.uid === ) // closeMenus(): void { // this.showColMenu = false; // this.showAreaMenu = false; // this.showStatusMenu = false; // } // async cargar(): Promise { // this.loading.set(true); // try { // const [ // aprendices, // areasData, // practicas, // todasMatriculas, // ] = await Promise.all([ // this.api.listarAprendices(), // this.api.listarAreas(), // this.api.listarPracticas(), // this.api.listarTodasMatriculas() // ]); // // 🔹 MAP prácticas por matrícula // const practicaMap = new Map (); // practicas.forEach((p: any) => { // const key = p.matriculaId ?? p.fk_matricula; // if (key != null) practicaMap.set(key, p); // }); // // 🔹 MAP matrículas por persona // const matriculasPorPersona = new Map (); // todasMatriculas.forEach((m: any) => { // const personaId = m.idPersona ?? m.id_persona ?? m.fk_persona; // if (personaId == null) return; // if (!matriculasPorPersona.has(personaId)) { // matriculasPorPersona.set(personaId, []); // } // matriculasPorPersona.get(personaId)!.push(m); // }); // // 🔄 TRANSFORMACIÓN FINAL — síncrona, sin llamadas extra por fila // const transformados = aprendices.map((persona: any) => { // const personaId = persona.idPersona ?? persona.id_persona; // const matriculas = matriculasPorPersona.get(personaId) || []; // const practica = matriculas // .map((m: any) => practicaMap.get(m.idMatricula ?? m.id_matricula)) // .find((p: any) => p != null) ?? null; // const primeraMatricula = matriculas[0] ?? null; // const curso = primeraMatricula?.curso ?? null; // // Avance viene directamente del campo guardado en etapa_practica // const avance = practica?.avance ?? 0; // return { // id: personaId, // name: persona.nombre, // age: persona.cedula, // email: persona.correo, // programa: curso?.programa?.nombre ?? , // area: curso?.area?.nombre ?? , // number: curso?.codigo ?? , // estado: practica?.estado ?? persona.estado, // startDate: (practica?.fecha_inicio ?? practica?.fechaInicio) // ? formatDate(practica.fecha_inicio ?? practica.fechaInicio) : , // endDate: (practica?.fecha_fin ?? practica?.fechaFin) // ? formatDate(practica.fecha_fin ?? practica.fechaFin) : , // avance, // observacion: practica?.observacion ?? , // id_practica: practica?.id ?? null, // }; // }); // this.data.set(transformados); // this.areas.set(areasData.map((a: any) => a.nombre)); // } catch (e: any) { // console.error( , e?.message ?? e); // this.data.set([]); // } finally { // this.loading.set(false); // } // } // // ── Helpers visuales ────────────────────────────────────────────────────── // initials(name: string): string { // return (name ?? ).split( ).slice(0, 2).map(n => n[0]).join( ).toUpperCase(); // } // avatarColor(name: string): string { // const palette = [ // , // , // , // ]; // return palette[(name?.charCodeAt(0) ?? 0) % palette.length]; // } // avanceValor(avance: any): number { return parseInt(avance) || 0; } // avanceColor(avance: any): string { // const v = this.avanceValor(avance); // return v >= 75 ? : v >= 40 ? : v > 0 ? ; // } // statusDotColor(uid: string): string { // const map: Record = { // activo: , inactivo: , suspendido: , // }; // return map[uid] ?? ; // } // // ── Acciones ────────────────────────────────────────────────────────────── // sort(col: string): void { // if (this.sortCol === col) { // this.sortDir = this.sortDir === ; // } else { // this.sortCol = col; // this.sortDir ="
  },
  {
    "file": "features\\seguimiento\\components\\toolbar\\aprendices-toolbar.component.ts",
    "content": "app-aprendices-toolbar"
  },
  {
    "file": "features\\seguimiento\\components\\toolbar\\column-selector.component.ts",
    "content": "app-column-selector Columnas @for(col of columns; track col.key){ }"
  },
  {
    "file": "features\\seguimiento\\components\\toolbar\\search-input.component.ts",
    "content": "app-search-input"
  },
  {
    "file": "features\\seguimiento\\modals\\crear-practica-modal.component.ts",
    "content": "@taiga-ui/core @taiga-ui/kit @taiga-ui/cdk practicaId practicaId Gestionar asignaciones app-crear-practica-modal tui-textfield { display: block; } // @if (isOpen) { // // // // // // // // // // // @if (modoEditar) { // Modifica los datos de la etapa práctica // } @else { // Completa los datos para asignar la etapa práctica // } // // // // // @if (loadingData()) { // Cargando datos… // } @else { // // @if (alumnoPreseleccionado) { // // // // // // // // // // · // // // // ID: // // // } // // @if (!modoEditar && !alumnoPreseleccionado) { // // Aprendiz // // // // } // // // Modalidad // // // // // // Empresa // // // // // // // Fecha inicio // // // // Fecha fin // // // // // @if (!modoEditar) { // // Instructor responsable (opcional) // // // // @if (mostrarListaInstructor && instructoresFiltrados().length > 0) { // // // @for (inst of instructoresFiltrados(); track inst.idPersona ?? inst.id_persona) { // // // // // // // } // // // } // @if (mostrarListaInstructor && // instructoresFiltrados().length === 0 && // instructorTexto.length > 0) { // // // Sin resultados para // // // } // // @if (form.instructorId) { // // Horas asignadas al instructor // // // } // } // // // Estado // // // // // // Observación (opcional) // // // // @if (error()) { // // } // } // // // // // Cancelar // // // @if (loading()) { // // } @else { // // } // // // // // } // ${a.name ?? } — ${a.programa ?? activo inactivo suspendido certificado por certificar Activo Inactivo Suspendido Certificado Por certificar isOpen aprendices activo YYYY-MM-DD ${d.year}-${mm}-${dd} YYYY-MM-DD Error cargando datos. activo Selecciona una modalidad. Selecciona una empresa. Ingresa la fecha de inicio. Ingresa la fecha de fin. string Error al guardar los cambios. Intenta de nuevo. Error al crear la etapa práctica. Intenta de nuevo. Selecciona un aprendiz. sin aprendiz El aprendiz no tiene matrícula registrada. sin matricula"
  },
  {
    "file": "features\\seguimiento\\modals\\editar-practica-modal.component.ts",
    "content": "@taiga-ui/core @taiga-ui/kit @taiga-ui/cdk app-editar-practica-modal tui-textfield { display: block; } @if (isOpen) { Editar etapa práctica @if (alumno) { · } @if (loadingData()) { Cargando datos… } @else { Modalidad Empresa Fecha inicio Fecha fin Estado Observación @if (error()) { } } Cancelar } activo inactivo suspendido certificado por certificar Activo Inactivo Suspendido Certificado Por certificar isOpen activo YYYY-MM-DD ${d.year}-${mm}-${dd} YYYY-MM-DD activo Error cargando datos. No se encontró la etapa práctica. Error al guardar los cambios. Intenta de nuevo."
  },
  {
    "file": "features\\seguimiento\\modals\\gestionar-asignaciones-modal.component.ts",
    "content": "@taiga-ui/core @taiga-ui/kit @taiga-ui/cdk app-gestionar-asignaciones-modal tui-textfield { display: block; } @if (isOpen) { Instructores asignados @if (alumno) { · } @if (cargando()) { } @else { @if (asignaciones().length === 0) { Esta etapa no tiene instructores asignados aún } @else { @for (a of asignaciones(); track a.id) { @if (!a.editando) { → &nbsp;·&nbsp; h @if (a.estado === ) { } @else { } } @else { Editando asignación Instructor @if (editMostrarLista && instructoresFiltradosEdit().length > 0) { @for (inst of instructoresFiltradosEdit(); track inst.idPersona ?? inst.id_persona) { } } Fecha inicio Fecha fin Horas Cancelar } } } @if (!mostrarFormNuevo()) { Agregar nuevo instructor } @else { Nueva asignación Instructor * @if (nuevoMostrarLista && instructoresFiltradosNuevo().length > 0) { @for (inst of instructoresFiltradosNuevo(); track inst.idPersona ?? inst.id_persona) { } } Fecha inicio * Fecha fin * Horas * @if (errorNuevo()) { } Cancelar } @if (error()) { } } Cerrar } isOpen activo Error cargando asignaciones. No se pudieron cargar las asignaciones. ${d}/${m}/${y} YYYY-MM-DD Asignación actualizada Los cambios fueron guardados correctamente. Error al guardar la asignación. [GestionarAsignaciones] Error al editar: Estado actualizado La asignación ahora está ${nuevoEstado}. Error al cambiar el estado. ¿Eliminar la asignación de Asignación eliminada La asignación de ${a.instructorNombre} fue eliminada. Error al eliminar la asignación. No se encontró la etapa práctica. Selecciona un instructor. Ingresa la fecha de inicio. Ingresa la fecha de fin."
  },
  {
    "file": "features\\seguimiento\\page-course\\page-course.component.ts",
    "content": "app-page-course ← Volver a áreas Cursos del Área + Nuevo curso @if (error()) { ⚠️ } @if (loading()) { } @if (!loading()) { @for (curso of cursos(); track curso.id_curso) { @if (curso.nombre) { } @if (curso.estado) { } } @if (cursos().length === 0) { No hay cursos registrados } } @if (modalOpen()) { @if (modalError()) { } Cancelar } idArea ID de área no válido. Error al cargar los cursos. El código es obligatorio. Curso actualizado El curso ${this.form.codigo} fue guardado. Curso creado El curso ${this.form.codigo} fue registrado correctamente. No se pudo guardar el curso. No se pudo guardar. ¿Seguro que deseas eliminar este curso? Curso eliminado El curso fue eliminado correctamente. No se pudo eliminar el curso."
  },
  {
    "file": "features\\seguimiento\\page-table\\aprendices.page.ts.ts",
    "content": "app-aprendices-page Administrativa Sistemas Contabilidad activo Activo practica Práctica egresado Egresado name Nombre area Área status Estado Juan Pérez Sistemas activo Laura Gómez Administrativa practica Carlos Ruiz Contabilidad egresado seguimientos observacion crear practica"
  },
  {
    "file": "features\\seguimiento\\seguimiento.component.ts",
    "content": "app-seguimiento"
  },
  {
    "file": "features\\seguimiento\\utils\\table.constants.ts",
    "content": "Nombre name Identificación Área area Programa programa Activo activo Inactivo inactivo Suspendido suspendido"
  },
  {
    "file": "features\\settings\\settings.component.ts",
    "content": "perfil password apariencia practicas sistema app-settings Perfil Contraseña Apariencia @if (esAdmin()) { Prácticas } @if (esAdmin()) { Sistema } @if (tab() === ) { Información Personal Actualiza tus datos de perfil @if (cargandoPerfil()) { } @else { Nombre completo Correo electrónico Teléfono Dirección Login (usuario) Cargo } } @if (tab() === ) { Cambiar Contraseña Por seguridad, ingresa tu contraseña actual Contraseña actual Nueva contraseña Confirmar nueva contraseña @if (pwd.nueva) { @for (s of [1,2,3,4]; track s) { } } } @if (tab() === ) { Apariencia Personaliza la interfaz a tu gusto Modo oscuro Cambia entre tema claro y oscuro Color de acento @for (t of temas; track t.id) { @if (temaActual() === t.id) { } } Tamaño de fuente Ajusta el tamaño del texto en la app A A A } @if (tab() === && esAdmin()) { Configuración de Prácticas Parámetros para la creación de etapas prácticas @if (cargandoConfig()) { } @else { Avance mínimo requerido Porcentaje mínimo de avance académico que debe tener un aprendiz para que el administrador pueda crearle una etapa práctica. 0% 50% 100% % = 70 ? : configMinAvance >= 40 ? #39A900 #f5a524 #f31260 > % Valor actual guardado: % . Un aprendiz con avance menor verá el botón bloqueado. } } @if (tab() === && esAdmin()) { Información del Sistema Datos de configuración de la plataforma 🏢 Plataforma EPSAS v1.0 🌐 Backend principal localhost:3000 ⚙️ Backend prácticas localhost:3001 🗄️ Base de datos PostgreSQL ⚡ Caché Redis 🔐 Autenticación JWT + Cookie Cerrar sesión } /* ── Layout ── */ .settings-wrap { max-width: 960px; margin: 0 auto; padding: 32px 16px; } .settings-header { display: flex; align-items: center; gap: 16px; background: white; border-radius: 20px; padding: 24px; box-shadow: 0 2px 16px rgba(0,0,0,.05); margin-bottom: 24px; border: 1px solid #f1f5f9; } .settings-avatar { width: 64px; height: 64px; border-radius: 50%; flex-shrink: 0; background: linear-gradient(135deg, #39A900, #2d8600); color: white; font-size: 22px; font-weight: 700; display: flex; align-items: center; justify-content: center; } .settings-name { font-size: 20px; font-weight: 700; color: #0f172a; margin: 0 0 6px; } .settings-badge { display: inline-block; font-size: 11px; font-weight: 600; padding: 3px 10px; border-radius: 20px; text-transform: capitalize; } .badge-administrador { background: #dcfce7; color: #16a34a; } .badge-instructor { background: #dbeafe; color: #1d4ed8; } .badge-aprendiz { background: #f3e8ff; color: #7c3aed; } .settings-body { display: grid; grid-template-columns: 200px 1fr; gap: 20px; align-items: start; } /* ── Nav ── */ .settings-nav { background: white; border-radius: 16px; padding: 8px; box-shadow: 0 2px 12px rgba(0,0,0,.05); border: 1px solid #f1f5f9; display: flex; flex-direction: column; gap: 2px; } .settings-nav button { width: 100%; display: flex; align-items: center; gap: 10px; padding: 10px 12px; border-radius: 10px; border: none; background: none; font-size: 13px; font-weight: 500; color: #64748b; cursor: pointer; transition: all .15s; text-align: left; } .settings-nav button svg { width: 16px; height: 16px; flex-shrink: 0; } .settings-nav button:hover { background: #f8fafc; color: #334155; } .settings-nav button.active { background: #39A900; color: white; } /* ── Panel ── */ .settings-panel { background: white; border-radius: 20px; padding: 28px; box-shadow: 0 2px 16px rgba(0,0,0,.05); border: 1px solid #f1f5f9; } .panel-title { font-size: 18px; font-weight: 700; color: #0f172a; margin: 0 0 4px; } .panel-sub { font-size: 13px; color: #94a3b8; margin: 0 0 24px; } .panel-footer { display: flex; justify-content: flex-end; margin-top: 24px; } /* ── Form ── */ .form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin-bottom: 8px; } .form-field label { display: block; font-size: 13px; font-weight: 600; color: #374151; margin-bottom: 6px; } .form-field input { width: 100%; padding: 10px 14px; border: 1.5px solid #e5e7eb; border-radius: 10px; font-size: 13px; color: #111827; outline: none; transition: border .15s; box-sizing: border-box; } .form-field input:focus { border-color: #39A900; box-shadow: 0 0 0 3px rgba(57,169,0,.1); } .form-field input.disabled { background: #f9fafb; color: #9ca3af; cursor: not-allowed; } /* ── Eye toggle ── */ .input-eye { position: relative; } .input-eye input { padding-right: 42px; } .input-eye button { position: absolute; right: 10px; top: 50%; transform: translateY(-50%); background: none; border: none; cursor: pointer; font-size: 16px; line-height: 1; } /* ── Strength ── */ .strength-wrap { display: flex; align-items: center; gap: 10px; margin: 12px 0; } .strength-bar { display: flex; gap: 4px; flex: 1; max-width: 200px; } .strength-seg { flex: 1; height: 4px; border-radius: 99px; background: #e5e7eb; transition: background .3s; } .strength-seg.filled { background: #39A900; } .strength-label { font-size: 12px; font-weight: 600; } .s1 .strength-seg.filled { background: #ef4444; } .s1 { color: #ef4444; } .s2 .strength-seg.filled { background: #f97316; } .s2 { color: #f97316; } .s3 .strength-seg.filled { background: #eab308; } .s3 { color: #eab308; } .s4 .strength-seg.filled { background: #22c55e; } .s4 { color: #22c55e; } /* ── Msgs ── */ .msg { margin-top: 12px; padding: 10px 14px; border-radius: 10px; font-size: 13px; font-weight: 500; } .msg-ok { background: #f0fdf4; color: #15803d; border: 1px solid #bbf7d0; } .msg-err { background: #fef2f2; color: #dc2626; border: 1px solid #fecaca; } /* ── Buttons ── */ .btn-primary { padding: 10px 24px; background: #39A900; color: white; border: none; border-radius: 10px; font-size: 13px; font-weight: 600; cursor: pointer; transition: opacity .15s; } .btn-primary:hover:not(:disabled) { opacity: .88; } .btn-primary:disabled { opacity: .5; cursor: not-allowed; } .btn-danger { padding: 10px 24px; background: #fee2e2; color: #dc2626; border: none; border-radius: 10px; font-size: 13px; font-weight: 600; cursor: pointer; transition: background .15s; } .btn-danger:hover { background: #fecaca; } /* ── Pref rows ── */ .pref-row { display: flex; align-items: center; justify-content: space-between; gap: 16px; padding: 4px 0; } .pref-label { font-size: 14px; font-weight: 600; color: #1e293b; margin: 0 0 2px; } .pref-desc { font-size: 12px; color: #94a3b8; margin: 0; } .divider { border: none; border-top: 1px solid #f1f5f9; margin: 20px 0; } /* ── Toggle ── */ .toggle { width: 48px; height: 26px; border-radius: 99px; border: none; cursor: pointer; background: #e2e8f0; position: relative; transition: background .25s; flex-shrink: 0; } .toggle.on { background: #39A900; } .toggle-knob { position: absolute; top: 3px; left: 3px; width: 20px; height: 20px; border-radius: 50%; background: white; transition: transform .25s; box-shadow: 0 1px 3px rgba(0,0,0,.2); } .toggle.on .toggle-knob { transform: translateX(22px); } /* ── Color chips ── */ .color-grid { display: flex; gap: 12px; flex-wrap: wrap; } .color-chip { width: 40px; height: 40px; border-radius: 50%; border: 3px solid transparent; cursor: pointer; display: flex; align-items: center; justify-content: center; transition: transform .15s, border-color .15s; } .color-chip:hover { transform: scale(1.1); } .color-chip.selected { border-color: #0f172a; } .color-chip svg { width: 18px; height: 18px; } /* ── Font size ── */ .font-size-btns { display: flex; gap: 8px; } .font-size-btns button { width: 36px; height: 36px; border-radius: 8px; border: 1.5px solid #e5e7eb; background: white; cursor: pointer; font-weight: 700; color: #374151; font-size: 12px; display: flex; align-items: center; justify-content: center; transition: all .15s; } .font-size-btns button.active { border-color: #39A900; background: #f0fdf4; color: #39A900; } /* ── Avance control ── */ .avance-control { display: flex; align-items: center; gap: 16px; width: 100%; max-width: 480px; } .avance-slider-wrap { flex: 1; } .avance-slider { width: 100%; -webkit-appearance: none; height: 6px; border-radius: 99px; background: #e5e7eb; outline: none; cursor: pointer; } .avance-slider::-webkit-slider-thumb { -webkit-appearance: none; width: 18px; height: 18px; border-radius: 50%; background: #39A900; cursor: pointer; border: 2px solid white; box-shadow: 0 1px 4px rgba(0,0,0,.2); } .avance-labels { display: flex; justify-content: space-between; font-size: 10px; color: #94a3b8; margin-top: 4px; } .avance-value-wrap { display: flex; align-items: center; gap: 4px; } .avance-number { width: 60px; padding: 8px 10px; border: 1.5px solid #e5e7eb; border-radius: 10px; font-size: 14px; font-weight: 700; text-align: center; color: #0f172a; outline: none; } .avance-number:focus { border-color: #39A900; box-shadow: 0 0 0 3px rgba(57,169,0,.1); } .avance-pct { font-size: 14px; font-weight: 600; color: #64748b; } .avance-preview { display: flex; align-items: center; gap: 10px; width: 100%; max-width: 480px; } /* ── Sistema ── */ .sys-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; } .sys-card { display: flex; align-items: center; gap: 12px; padding: 14px 16px; background: #f8fafc; border-radius: 12px; border: 1px solid #f1f5f9; } .sys-icon { font-size: 22px; } .sys-label { font-size: 11px; color: #94a3b8; margin: 0 0 2px; } .sys-value { font-size: 14px; font-weight: 600; color: #1e293b; margin: 0; } /* ── Spinner ── */ .spinner-wrap { display: flex; justify-content: center; padding: 40px; } .spinner { width: 32px; height: 32px; border: 4px solid rgba(57,169,0,.2); border-top-color: #39A900; border-radius: 50%; animation: spin .8s linear infinite; } @keyframes spin { to { transform: rotate(360deg); } } /* ── Responsive ── */ @media (max-width: 640px) { .settings-body { grid-template-columns: 1fr; } .form-grid { grid-template-columns: 1fr; } .sys-grid { grid-template-columns: 1fr; } } perfil dark tema verde fontSize normal /api/persona/buscar_jwsv/${personaId} /api/persona/buscar_jwsv/${personaId} /api/persona/actualizar_jwsv/${personaId} Perfil actualizado Los cambios fueron guardados correctamente. Error al guardar el perfil. Campos requeridos Completa todos los campos. Contraseñas distintas Las contraseñas nuevas no coinciden. No se pudo cargar la configuración. Configuración guardada Avance mínimo establecido en ${v}%. Error al guardar la configuración."
  },
  {
    "file": "layout\\floating-buttons\\floating-buttons.spec.ts",
    "content": "FloatingButtons should create"
  },
  {
    "file": "layout\\floating-buttons\\floating-buttons.ts",
    "content": "ngx-lottie app-floating-buttons robot.json usuario asistente asistente ¡Hola! Soy tu asistente virtual. ¿En qué puedo ayudarte con la plataforma? asistente ⏳ ¿Sigues ahí? Si no hay respuesta en 1 minuto, la conversación se cerrará automáticamente. asistente 🔄 La conversación ha finalizado por inactividad. ¡Hola de nuevo! Soy tu asistente virtual. ¿En qué puedo ayudarte? $1 $2 usuario asistente Sin respuesta asistente Ocurrió un error, intenta de nuevo. ngx-lottie app-floating-buttons robot.json usuario asistente asistente ¡Hola! Soy tu asistente virtual. ¿En qué puedo ayudarte con la plataforma? 1. texto $1 $2 usuario asistente Sin respuesta asistente Ocurrió un error, intenta de nuevo."
  },
  {
    "file": "layout\\footer\\footer.component.ts",
    "content": "lucide-angular app-footer © EPSAS — Todos los derechos reservados Calle 123 #45-67, Bogotá, Colombia Política de privacidad Términos de uso"
  },
  {
    "file": "layout\\main-layout\\main-layout.component.ts",
    "content": "app-main-layout"
  },
  {
    "file": "layout\\navbar\\navbar.component.ts",
    "content": "app-navbar EPSAS @if (esAdmin()) { } EPSAS Usuario administrador"
  },
  {
    "file": "layout\\navbar\\notificaciones-campana.component.ts",
    "content": "app-notificaciones-campana @if (unread() > 0) { } @if (open()) { Notificaciones @if (unread() > 0) { Marcar todas como leídas } @if (cargando()) { } @else if (notificaciones().length === 0) { 🔔 Sin notificaciones nuevas } @else { @for (n of notificaciones(); track n.id) { @if (!n.leida) { } @else { } @if (n.tipo === && n.data?.[ ]?.length) { @for (a of n.data![ ].slice(0, 8); track a.cedula) { · % } @if (n.data![ ].length > 8) { + más… } } } } } document:click $event app-notificaciones-campana es-CO 2-digit short numeric 2-digit 2-digit"
  },
  {
    "file": "layout\\sidebar\\sidebar.component.ts",
    "content": "app-sidebar @if (open) { } @for (group of visibleGroups; track group.id; let first = $first) { @if (open) { } @else { @if (!first) { } } @for (link of group.links; track link.href) { @if (open) { } @else { } } } @if (open) { Cerrar sesión } /* ── Links ───────────────────────────────────────────────── */ .nav-link { position: relative; display: flex; align-items: center; gap: 10px; padding: 9px 10px 9px 11px; border-radius: 8px; font-size: 13px; font-weight: 500; color: #2B2F38; text-decoration: none; cursor: pointer; transition: background .15s, color .15s; border-left: 3px solid transparent; } .nav-link:hover { background: #f8fafc; color: #1e293b; } /* Estado activo — ThemeService sobreescribe los colores via !important */ .nav-link-active { background: rgba(0, 120, 50, .08); color: #007832; border-left-color: #007832; font-weight: 600; } /* ── Encabezados de sección ─────────────────────────────── */ .nav-section-label { font-size: 10px; font-weight: 700; letter-spacing: .09em; text-transform: uppercase; color: #656E85; padding: 0 13px; margin: 14px 0 3px; } .nav-section-label.mt-0 { margin-top: 4px; } /* ── Tooltip (modo colapsado) ────────────────────────────── */ .nav-tooltip { position: absolute; left: calc(100% + 10px); top: 50%; transform: translateY(-50%); background: #1e293b; color: #fff; font-size: 12px; font-weight: 500; padding: 5px 10px; border-radius: 7px; white-space: nowrap; opacity: 0; pointer-events: none; transition: opacity .15s; z-index: 9999; box-shadow: 0 4px 12px rgba(0,0,0,.15); } /* Flecha del tooltip */ .nav-tooltip::before { content: ; position: absolute; right: 100%; top: 50%; transform: translateY(-50%); border: 5px solid transparent; border-right-color: #1e293b; } .nav-link:hover .nav-tooltip { opacity: 1; } /* ── Botón cerrar sesión ─────────────────────────────────── */ .nav-logout { display: flex; align-items: center; gap: 10px; width: 100%; padding: 9px 10px; border-radius: 8px; border: none; background: none; font-size: 13px; font-weight: 500; color: #94a3b8; cursor: pointer; transition: background .15s, color .15s; font-family: inherit; } .nav-logout:hover { background: #fef2f2; color: #dc2626; } /* ── Versión ─────────────────────────────────────────────── */ .nav-version { font-size: 10px; color: #e2e8f0; font-weight: 600; letter-spacing: .06em; text-align: center; margin-top: 6px; } principal Principal Inicio /home Seguimiento /seguimiento modulos Módulos Formatos /format Historial /docs administrador Migración /migracion administrador sistema Sistema Admin /admin administrador Configuración /settings Usuario"
  },
  {
    "file": "shared\\directives\\has-role.directive.ts",
    "content": "[appHasRole] _injector"
  },
  {
    "file": "shared\\models\\estudiante.model.ts",
    "content": "Historial Activo Inactivo Graduado Aprobado Reprobado En curso"
  }
]